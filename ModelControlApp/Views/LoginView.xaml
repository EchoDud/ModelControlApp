<Window x:Class="ModelControlApp.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="200" Width="300" WindowStartupLocation ="CenterOwner" ResizeMode="NoResize">
    <Grid>
        <StackPanel>
            <Label Content="Username"/>
            <TextBox x:Name="Username" Text="{Binding Username}"/>
            <Label Content="Password"/>
            <PasswordBox x:Name="Password" PasswordChanged="PasswordBoxPasswordChanged"/>
            <Button Content="Login" Command="{Binding LoginCommand}"/>
        </StackPanel>
    </Grid>
</Window>